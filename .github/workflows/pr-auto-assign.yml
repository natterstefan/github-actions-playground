# @see https://github.com/kentaro-m/auto-assign-action
name: PR Auto Assign

on:
  workflow_call:
  workflow_dispatch:
  pull_request:
    types: [opened, ready_for_review]

permissions:
  contents: read

jobs:
  check-auto-assign:
    permissions:
      pull-requests: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: kentaro-m/auto-assign-action@9c908d3e3e5186345e157ed6161d95b5652f71e9
